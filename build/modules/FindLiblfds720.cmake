FIND_PATH(LIBLFDS720_INCLUDE_DIR NAMES liblfds720.h PATHS ${ROOT}/lib/liblfds7.2.0/src/liblfds720/inc NO_DEFAULT_PATH)
FIND_LIBRARY(LIBLFDS720_LIBRARIES NAMES lfds720 PATHS ${ROOT}/lib/liblfds7.2.0/src/liblfds720/bin NO_DEFAULT_PATH)

MESSAGE(STATUS "${LIBLFDS720_LIBRARIES}")

IF(LIBLFDS720_INCLUDE_DIR AND LIBLFDS720_LIBRARIES)
	SET(LIBLFDS720_FOUND TRUE)
ENDIF(LIBLFDS720_INCLUDE_DIR AND LIBLFDS720_LIBRARIES)

IF(LIBLFDS720_FOUND)
	IF (NOT Liblfds720_FIND_QUIETLY)
		MESSAGE(STATUS "Found liblfds720 includes: ${LIBLFDS720_INCLUDE_DIR}/liblfds720.h")
		MESSAGE(STATUS "Found liblfds720 library: ${LIBLFDS720_LIBRARIES}")
	ENDIF (NOT Liblfds720_FIND_QUIETLY)
ELSE(LIBLFDS720_FOUND)
	IF (Liblfds720_FIND_REQUIRED)
		MESSAGE(FATAL_ERROR "Could NOT find liblfds720 development files")
	ENDIF (Liblfds720_FIND_REQUIRED)
ENDIF(LIBLFDS720_FOUND)
